<!DOCTYPE html>
<html>
	<%= render 'layouts/header' %>
	<body>
		<%= javascript_include_tag "https://www.gstatic.com/charts/loader.js", "chartkick" %>
		<!--Note from Carolyn C - logged in pages all have this container col structure-->
		<% if logged_in? %>
			<%= render 'layouts/navbar1' unless @disable_nav %>
			<div class="container">
				<div class="col-xs-10 col-xs-offset-1 col-sm-12 col-sm-offset-0 content-col">
		<% end %>

		<%= yield %>

		<!-- Note from Carolyn C- end the divs -->
		<% if logged_in? %>
			</div>
		</div>
		<% end %>
		<%= render 'layouts/footer' %>
		<!-- Another note from Carolyn C: again, if you are working on a page that you see while NOT logged in, you need to manually add the container and the div associated.-->

		<%= debug(params) if Rails.env.development? %>
	</body>
</html>